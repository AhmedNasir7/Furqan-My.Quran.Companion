<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Model.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Demo">
        <activity
            android:name=".Activities.DuaDetailActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.PrayerDetailActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.TafseerActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.BookmarksActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.ReadPage"
            android:exported="false" />
        <activity
            android:name=".Activities.TasbeehCounterPage"
            android:exported="false" />
        <activity
            android:name=".Activities.SignUpPage"
            android:exported="false" />
        <activity
            android:name=".Activities.LoginPage"
            android:exported="false" />
        <activity
            android:name=".Activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

    </application>

</manifest>